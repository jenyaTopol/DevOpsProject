apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-flask-mysql.fullname" . }}
  labels:
    {{- include "python-flask-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-flask-mysql.selectorLabels" . | nindent 4 }}
